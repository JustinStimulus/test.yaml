kubectl patch deployment stimulus-web -n stimulus-uat --type='json' -p='[
  {
    "op": "replace",
    "path": "/spec/template/spec/containers/0/args",
    "value": [
      "bash", "-c", 
      "echo \"🔧 Fixing environment variables...\"; echo \"Current values: BACKEND=$NEXT_PUBLIC_AGENTS_BACKEND_URL DEPLOYMENT=$NEXT_PUBLIC_AGENTS_DEPLOYMENT_URL\"; find .next -name \"route.js\" -path \"*/copilotkit/*\" | xargs sed -i \"s|NEXT_PUBLIC_AGENTS_BACKEND_URL:\\\"\\\"|NEXT_PUBLIC_AGENTS_BACKEND_URL:\\\"$NEXT_PUBLIC_AGENTS_BACKEND_URL\\\"|g; s|NEXT_PUBLIC_AGENTS_DEPLOYMENT_URL:\\\"\\\"|NEXT_PUBLIC_AGENTS_DEPLOYMENT_URL:\\\"$NEXT_PUBLIC_AGENTS_DEPLOYMENT_URL\\\"|g; s|process\\.env\\.NEXT_PUBLIC_AGENTS_BACKEND_URL||\\\"$NEXT_PUBLIC_AGENTS_BACKEND_URL\\\"|g; s|process\\.env\\.NEXT_PUBLIC_AGENTS_DEPLOYMENT_URL||\\\"$NEXT_PUBLIC_AGENTS_DEPLOYMENT_URL\\\"|g\"; echo \"✅ Replacement complete. Starting Next.js...\"; exec bun run next start"
    ]
  }
]'
